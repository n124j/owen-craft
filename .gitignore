# Real Firebase config lives here — never commit it
.env

# Build output (generated by build.js, contains the injected config)
dist/

# Manually built copy with real config baked in — redundant now that
# build.js generates dist/index.html; safe to delete
index.html
